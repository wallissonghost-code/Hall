// SECURITY: o catálogo NÃO existe no DOM/HTML antes da autorização.
// Em produção, verifySubscription() deve chamar o backend NOT e receber apenas os jogos permitidos.
async function verifySubscription(){return {ok:false};}
const gate=document.querySelector("#gate"),app=document.querySelector("#app"),verify=document.querySelector("#verify"),statusEl=document.querySelector("#gateStatus");
verify.onclick=async()=>{verify.disabled=true;statusEl.textContent="Confirmando assinatura...";try{const session=await verifySubscription();if(!session?.ok){statusEl.textContent="Assinatura ainda não conectada ao backend.";return}mountHall(session.games||[])}catch(e){statusEl.textContent="Não foi possível confirmar a assinatura."}finally{verify.disabled=false}};
function mountHall(games){gate.remove();app.hidden=false;app.innerHTML=`<header><a class="logo">NOT<span>.</span></a><nav><button class="active" data-filter="all">Todos</button><button data-filter="live">Live</button><button data-filter="casual">Casual</button><button data-filter="new">Novos</button></nav><div class="right"><div class="avatar">N</div></div></header><main><section class="welcome"><div><small>NOT GAMES</small><h1>Escolha um jogo</h1><p>Seu catálogo de jogos em um só lugar.</p></div><label class="search">⌕ <input id="search" placeholder="Buscar jogo..."></label></section><section><div class="sectionTitle"><h2>Jogos</h2><span id="count"></span></div><div class="grid" id="games"></div><div class="empty" id="empty">Nenhum jogo encontrado.</div></section></main>`;let filter="all";const grid=app.querySelector("#games"),search=app.querySelector("#search"),count=app.querySelector("#count"),empty=app.querySelector("#empty");function render(){const q=search.value.toLowerCase().trim(),list=games.filter(g=>(filter==="all"||g.cat===filter)&&g.name.toLowerCase().includes(q));grid.innerHTML=list.map(g=>`<article class="game"><div class="cover"><span class="tag">${g.tag||""}</span><div class="mark">NOT</div><span class="play">▶</span></div><h3>${g.name}</h3><p>${g.desc||""}</p></article>`).join("");count.textContent=`${list.length} disponíveis`;empty.style.display=list.length?"none":"block"}app.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{app.querySelector("nav .active").classList.remove("active");b.classList.add("active");filter=b.dataset.filter;render()});search.oninput=render;render()}